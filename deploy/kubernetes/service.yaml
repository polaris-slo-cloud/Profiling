apiVersion: v1
kind: Service
metadata:
  name: polaris-profiler
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: polaris-profiler
  ports:
    - name: http
      port: 80
      targetPort: http
